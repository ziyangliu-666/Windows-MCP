# Result Log 2026-03-10 14:48 CST

## Scope
- Verify the live session after the persistent stdio worker bridge was activated by another host reconnect.
- Confirm whether the forwarded long-call defect is gone in the connected MCP session.
- Separate any remaining `DevServer` failure from worker transport reliability.

## Live Results

### Forwarded Long Call
- Tool: `PowerShell`
- Action: `Start-Sleep -Seconds 8; Write-Output 'live-persist-stdio-ok'`
- Result: pass
- Interpretation:
  - the live session is no longer using the old unreliable worker HTTP path for this benchmark;
  - the persistent stdio bridge fix is active and effective in the external MCP session.

### Normal Forwarded/UI Paths
- `Snapshot`: pass
- `App.launch("Calculator")`: pass and verified by fresh `Snapshot`
- `App.switch("Notepad")`: fail, but returned failure honestly while Ubuntu remained foreground

### DevServer
- `DevServer(mode="health")`: still fail with outer 120 second timeout
- Interpretation:
  - this is now isolated as a separate host-facing integration problem;
  - it no longer tracks worker transport health.

## Conclusion
- The persistent stdio bridge is verified live for the main reliability target: longer-running forwarded calls now return successfully.
- Normal forwarded/UI behavior still works on the same live shell.
- `DevServer` remains the only live failing surface observed in this checkpoint.
