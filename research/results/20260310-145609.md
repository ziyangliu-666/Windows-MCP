# Result Log: 2026-03-10 14:56:09 CST

## Goal
- Re-check the remaining live `App.switch` uncertainty under controlled focus.
- Prepare a decisive experiment for the remaining live `DevServer` timeout.

## Actions
1. Confirmed the live shell is still healthy for non-`DevServer` calls:
   - `Clipboard(mode="get")` returned immediately.
   - forwarded `PowerShell("Start-Sleep -Seconds 8; Write-Output 'ps8-ok'")` returned successfully.
   - `DevServer(mode="health")` still hit the outer 120 second tool timeout.
2. Re-ran app switching with a clean precondition:
   - observed both Notepad and Calculator already open via `Process.list` and `Snapshot`;
   - called `App.switch("Notepad")`;
   - re-observed with `Snapshot` and verified Notepad was foreground;
   - called `App.switch("Calculator")`;
   - re-observed with `Snapshot` and verified Calculator was foreground.
3. Added local shell-side `DevServer` trace logging in `src/windows_mcp/dev_hot.py`:
   - `dev_invoker_enter`
   - `dev_invoker_ok` / `dev_invoker_error`
   - `dev_server_sync_enter`
   - `dev_server_sync_health_ok` / reload variants
4. Added targeted regression coverage in `tests/test_dev_hot.py` and verified:
   - `11 passed in 5.25s`

## Findings
- The previous post-transport `App.switch("Notepad")` failure is no longer reproducible under controlled focus and is best classified as environmental focus contention, not a current switch-logic regression.
- The remaining live reliability gap is still isolated to `DevServer`.
- The new trace patch is the next highest-value experiment, but it requires one more shell restart because `DevServer` is shell-owned.

## Evidence
- Live `Clipboard`: pass
- Live forwarded `PowerShell Start-Sleep 8`: pass
- Live `DevServer.health`: fail with outer 120 second timeout
- Live `App.switch("Notepad")`: pass with snapshot-verified foreground
- Live `App.switch("Calculator")`: pass with snapshot-verified foreground

## Next Step
- Restart the hot shell once more, trigger exactly one live `DevServer(mode="health")`, and inspect `research/runtime/devserver_trace.jsonl` to classify whether the timeout reaches shell code at all.
