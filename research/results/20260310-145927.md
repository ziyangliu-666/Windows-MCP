# Result Log: 2026-03-10 14:59:27 CST

## Goal
- Re-check whether live Notepad typing still has residual flakiness after the earlier keyboard-path fix.

## Actions
1. Switched focus back to the existing Notepad window and captured a fresh `Snapshot`.
2. Verified the precondition from informative text:
   - `13 个字符`
   - caret at `列 14`
3. Attempted one append with the documented coordinate-string shape:
   - `Type(loc="500,400", text="x")`
4. Recorded the validation failure, retried once with the numeric-list shape:
   - `Type(loc=[500,400], text="x")`
5. Captured a fresh `Snapshot` and verified:
   - `14 个字符`
   - caret at `列 15`
6. Repeated the same append once more with `Type(loc=[500,400], text="y")`.
7. Captured a fresh `Snapshot` and verified:
   - `15 个字符`
   - caret at `列 16`

## Findings
- The current live typing path is stable across repeated appends and no longer shows the earlier dropped-character symptom.
- A new reliability issue exists at the tool-contract layer: live `Type.loc` requires a numeric list and rejects the documented string coordinate form.

## Classification
- Typing stability: pass
- Tool contract: fail
- Failure bucket: `tool schema / contract mismatch`

## Next Step
- Keep `Desktop.type()` low-priority for now and focus on the remaining `DevServer` timeout after the next shell restart; then audit coordinate-tool contracts so the Codex-facing surface matches the live server schema.
