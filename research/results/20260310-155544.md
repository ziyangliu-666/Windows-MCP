# Result Log: 2026-03-10 15:55:44 CST

## Goal
- Turn the Settings routing finding into a concrete product-path improvement by teaching `App.launch` to accept protocol targets directly.

## Change
- `Desktop.launch_app()` now detects protocol targets such as `ms-settings:bluetooth`.
- Protocol targets bypass Start Menu fuzzy matching and are launched directly with `Start-Process`.

## Verification
### Unit tests
- `tests/test_app_service.py`
  - pass
  - added protocol-target coverage

### In-process MCP behavioral check
- Action:
  - `App(mode="launch", name="ms-settings:bluetooth")`
- Result:
  - pass
  - returned `Settings launched.`
- Follow-up observe:
  - `Snapshot(use_vision=false)`
- Verification:
  - focused window was `Settings`
  - interactive and informative text both showed `Bluetooth & devices`

### Repo regression check
- `177 passed in 5.49s`

## Takeaway
- The stronger Settings route is now available through the main `App.launch` path locally.
- Live verification still needs the shell to be restarted or reconnected because this launcher path is shell-owned in hot mode.
