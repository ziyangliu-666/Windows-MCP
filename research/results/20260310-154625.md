# Result Log: 2026-03-10 15:46:25 CST

## Goal
- Expand live benchmark coverage beyond hot-shell recovery into Explorer, terminal, browser DOM, and a mixed browser-to-desktop flow.

## Results
### Explorer navigation
- Action: opened `C:\\Users\\Rufus\\src\\Windows-MCP\\research` with `explorer.exe`
- Verification:
  - focused window title matched the target path
  - Shell.Application window enumeration reported `C:\\Users\\Rufus\\src\\Windows-MCP\\research`

### Terminal verification
- Action: ran one repo-scoped `PowerShell` command
- Verification:
  - `results_count=17`
  - `cwd=C:\\Users\\Rufus\\src\\Windows-MCP`

### Browser DOM extraction
- First result: fail
  - `Scrape(use_dom=true)` crashed with `'ScrollElementNode' object has no attribute 'vertical_scroll_percent'`
- Patch:
  - fixed `scrape_handler()` to read `vertical_scroll_percent` from `dom_node.metadata`
- Live recheck after `DevServer.reload`:
  - pass
  - returned `Example Domain` content from the active Chrome tab

### Mixed browser + desktop handoff
- Browser stage:
  - DOM extraction produced `Example Domain`
- Desktop handoff:
  - `App.switch("Notepad")` failed honestly from Chrome foreground
  - taskbar-button recovery restored Notepad focus
  - typing `Example Domain` succeeded
- Verification:
  - Notepad advanced from `16 个字符` to `30 个字符`
  - caret moved to `列 31`

## Takeaway
- The routing strategy is holding:
  - Explorer and terminal tasks are strongest on shell/process paths
  - browser extraction is strongest on DOM paths
  - mixed workflows need explicit focus recovery instead of trusting app-switch success strings
