# Result Log: 2026-03-10 16:45:11 CST

## Goal
- Re-test focus recovery after the `bring_window_to_top()` fallback patch against a non-browser foreground app.

## Preconditions
- `Settings` open on `Bluetooth & devices`
- Notepad window `*wmcp-type-probe-1773117083.txt - Notepad` already open

## Steps
1. Observed desktop state
   - actual foreground was `Ubuntu`, so this was not yet a valid recovery setup
2. Brought `Settings` to foreground using the visible taskbar button
3. Re-observed and verified `Settings` foreground
4. Called `App.switch("Notepad")`
5. Re-observed and verified Notepad foreground
6. Typed one character with `Type(loc="500,400", text="R")`
7. Re-observed and verified Notepad character count increased from `33` to `34`

## Result
- Pass

## Evidence
- Post-switch snapshot:
  - foreground `*wmcp-type-probe-1773117083.txt - Notepad`
- Post-type snapshot:
  - foreground remained Notepad
  - informative text changed to `34 个字符`

## Interpretation
- The worker-side post-Win32 fallback fix is not limited to the original Chrome-to-Notepad failure.
- At least one additional non-browser foreground path now succeeds without a supervisor-level taskbar fallback.
- The remaining question is not whether `App.switch` is still broadly broken; it is whether any app classes still need a higher-level recovery ladder after the worker fix.
