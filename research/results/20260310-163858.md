# Result Log: 2026-03-10 16:38:58 CST

## Goal
- Close the hard download-and-verify benchmark cleanly.
- Improve pre-restart shell diagnostics so a stale native shell can still report usable identity and restart state.

## Investigation
### Native versus dynamic restart diagnostics
- Live `DevServer(mode="call", name="list_calls", load_latest=false)` still exposed only the older native calls:
  - `describe_restart_boundary`
  - `list_calls`
- The same shell, with `load_latest=true`, loaded the on-disk dynamic calls successfully and exposed:
  - `describe_shell_file_delta`
  - `probe_dynamic_reload`
- That confirms a practical split:
  - the connected shell process is still on the older native call table;
  - dynamic shell calls remain the reliable pre-restart escape hatch.

### Download benchmark completion
- Fresh desktop observe showed Chrome foreground on a `127.0.0.1` network error page.
- Filesystem verification showed `C:\Users\Rufus\Downloads\benchmark-download-20260310-163005.bin` already existed.
- Reading the file returned the expected content:
  - `windows-mcp download benchmark`
- Launching `explorer.exe /select,...` focused `C:\Users\Rufus\Downloads - File Explorer`.
- The follow-up snapshot showed:
  - the benchmark file selected
  - status text `1 item selected  31 bytes`

## Patch
- `health_snapshot()` and native restart-boundary diagnostics now expose shell runtime identity:
  - `shell_pid`
  - `shell_session_id`
  - `shell_started_at_epoch`
  - `shell_started_at_iso`
  - `shell_python_executable`
- Dynamic `describe_shell_file_delta` now also exposes:
  - runtime identity
  - `shell_loaded_source_hash`
  - `shell_current_source_hash`
  - `shell_restart_required`
  - `reconnect_required`
- The dynamic path now synthesizes `shell_session_id` from PID plus start time when the stale shell process does not provide one natively.

## Verification
### Local
- `./.venv/Scripts/python.exe -m pytest tests/test_dev_hot.py -q`
  - pass (`18 passed`)
- `./.venv/Scripts/python.exe -m pytest -q`
  - pass (`184 passed`)

### Live
- `DevServer(mode="call", name="describe_shell_file_delta", load_latest=true)`
  - pass
  - returned:
    - `shell_pid = 25876`
    - `shell_session_id = 25876-1773130197867`
    - `shell_started_at_iso = 2026-03-10T08:09:57Z`
    - `shell_loaded_source_hash = 7c72e5f8...`
    - `shell_current_source_hash = 1d8d6049...`
    - `shell_restart_required = true`
- Hard download benchmark
  - pass
  - filesystem content matched
  - Explorer verification matched

## Reliability Takeaways
- Browser window state alone was misleading in this benchmark: Chrome still showed a failed localhost page while the actual downloaded artifact already existed in `Downloads`.
- For browser download tasks, filesystem-plus-Explorer verification is stronger than trusting the active tab title.
- For shell restarts, native-call presence is not a sufficient oracle unless it is paired with explicit shell identity.
