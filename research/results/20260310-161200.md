# Result Log: 2026-03-10 16:12:00 CST

## Goal
- Add a shell-native dynamic call surface so future shell diagnostics can be exercised before another shell restart.

## Change
- `DevServer` now supports `mode="call"` with:
  - `name`
  - `arguments_json`
  - `load_latest`
- Native calls are built into the shell.
- Dynamic calls are loaded from `src/windows_mcp/dev_shell_calls.py` on every call when `load_latest=true`.

## Verification
### Targeted tests
- `tests/test_dev_hot.py`
  - pass
  - verified:
    - public `DevServer` tool forwards dynamic-call arguments
    - `list_calls` exposes native and dynamic call targets
    - rewriting the dynamic module on disk changes call behavior without restarting the shell

### Full regression
- `179 passed in 5.53s`

## Takeaway
- This establishes a stable shell control surface:
  - restart adds native `DevServer(mode="call")` support
  - future shell diagnostics placed in `src/windows_mcp/dev_shell_calls.py` can be exercised before the next restart boundary
