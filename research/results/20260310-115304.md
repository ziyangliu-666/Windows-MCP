# Reliability Results Log

Timestamp: 20260310-115304

## Session Goal
- Improve observability and stale-state handling after stabilizing keyboard input and app focus logic.
- Validate the actual MCP tool layer against patched repo code.

## Benchmarks

### Snapshot Informative Text
- Change:
  - Native informative text is now retained in `TreeState.informative_nodes`.
  - `Snapshot` output now includes `List of Informative Text Elements`.
- Verification:
  - Local Calculator state exposed informative items including `Display is 0`.
  - After clicking `Seven`, `Plus`, `Eight`, `Equals`, the follow-up state exposed `Display is 15`.
- Outcome:
  - Verification-heavy apps can now be checked from state alone instead of custom UIA readback scripts.

### Full Test Suite
- Change:
  - Restored `Desktop._ps_quote()` as a compatibility shim after broad test execution exposed unrelated registry-test failures.
- Verification:
  - Windows-side `uv.exe run --extra dev pytest -q`
- Outcome:
  - Full suite passed: `158 passed in 1.56s`.

### Tool-Layer Freshness Guard
- Change:
  - `DesktopState` now tracks `captured_at_epoch`.
  - `Snapshot` emits a timestamp.
  - Label-based tools require a desktop-state age of at most 10 seconds.
- Verification:
  - Local in-process MCP call to `Snapshot` showed the new timestamp and informative text section.
  - After manually aging the cached state by 11 seconds, label-based `Click` raised `Desktop state is 11.0s old. Please call Snapshot again.`
- Outcome:
  - The observe-act-verify loop now has an enforceable freshness boundary for label-driven actions.

## Regression Found And Fixed
- Freshness patch regression:
  - `Snapshot` initially failed with `UnboundLocalError` because `captured_at_epoch` used `end_time` before assignment.
  - Fixed by assigning capture time directly from `time()` before constructing `DesktopState`.

## Artifacts Added
- `research/experiments/local_mcp_tool_call.py`
- `tests/test_desktop_state_freshness.py`

## Current Conclusion
- Tool-layer behavior can now be validated locally through an in-process FastMCP harness.
- Snapshot observability and stale-state enforcement are materially improved.
- The main unresolved operational issue is still the stale live MCP server connection and whether it can be reloaded safely in-session.
