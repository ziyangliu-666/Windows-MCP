# Reliability Results Log

Timestamp: 20260310-114333

## Session Goal
- Continue the active reliability loop from the existing workspace.
- Verify the keyboard patch against patched local code.
- Patch and verify `App.launch` / `App.switch` postcondition correctness.

## Benchmarks

### Keyboard Probe
- Tooling:
  - `research/experiments/notepad_type_probe.py`
- Findings:
  - Process inspection showed the live MCP server processes were started before the `_INPUTUnion` patch, so MCP-tool `Type` behavior remained stale.
  - First local probe after the patch no longer crashed, but read back `wmcp yboard probe ...` instead of `wmcp keyboard probe ...`.
  - Six follow-up trials passed:
    - 3 with `--pre-type-delay 0`
    - 3 with `--pre-type-delay 0.2`
- Outcome:
  - The deterministic keyboard crash is fixed.
  - There may still be a low-frequency focus/timing race, but it did not reproduce in the controlled follow-up set.

### Launch / Switch Probe
- Tooling:
  - `research/experiments/app_focus_probe.py`
- Findings before fallback patch:
  - `switch(Notepad)` from Calculator surfaced `AttachThreadInput` access denied and correctly returned failure instead of false success.
- Code changes:
  - Added launch verification helpers that use observed window/process state.
  - Added foreground verification in `switch_app()`.
  - Added fallback focus path in `bring_window_to_top()` for `AttachThreadInput` denial.
- Findings after patch:
  - `launch(记事本)` returned `无标题 - Notepad launched.` and the foreground window became `无标题 - Notepad`.
  - `launch(Calculator)` returned `Calculator launched.` with Calculator foreground.
  - `switch(Notepad)` from Calculator returned success and the foreground window became `无标题 - Notepad`.

## Code Artifacts Added
- `research/experiments/notepad_type_probe.py`
- `research/experiments/app_focus_probe.py`
- `tests/test_app_service.py`

## Test Execution Notes
- `compileall` passed for modified source and experiment files.
- `pytest` could not be run from the local `.venv` because `pytest` is not installed there.
- `uv` is not available on the WSL shell PATH, so test execution needs a Windows-side path or environment fix.

## Current Conclusion
- Keyboard input is no longer blocked by the `_INPUTUnion` bug.
- Launch and switch reporting are materially more trustworthy in the patched local code path.
- Observability is now the main bottleneck: `Snapshot` still hides informative text needed for direct result verification.
