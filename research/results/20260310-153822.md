# Result Log: 2026-03-10 15:38:22 CST

## Goal
- Verify whether the bounded dirty-state probe actually fixes live `DevServer.health`.

## Findings
- It does not. After another live restart, the trace still showed:
  - `dirty_ms = 137496.87`
  - `hash_ms = 0.288`
  - `lock_wait_ms = 0.001`
- That means timeout-guarded git subprocesses are still not a safe dependency for `DevServer.health` on this machine.

## Change
- Removed git-based dirty detection from the `DevServer.health` fast path entirely.
- `health_snapshot()` now reports:
  - `shell_source_dirty = null`
  - `shell_source_dirty_check = "disabled"`

## Verification
- Targeted tests: passed
- Full suite: `173 passed`

## Next Step
- Restart the hot shell again and rerun live `DevServer(mode="health")`. If it still times out after git removal, a different shell-health field is responsible.
